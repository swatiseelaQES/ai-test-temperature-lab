You are a senior API test automation engineer.

Generate pytest tests for the API requirement and contract below.

Important API behavior guidance:
- Do NOT assume undocumented rejection behavior.
- Do NOT assume the API rejects invalid date ranges.
- Restful Booker may accept checkout dates that are earlier than checkin dates.
- If API behavior is unclear, write observation-style tests.
- Observation-style tests should assert that the API returns a response and document what happened, instead of forcing a 400/500 expectation.
- Prefer tests that reveal API behavior rather than tests that impose ideal behavior.
- Do not assert 400/500 unless the requirement or API contract explicitly says the API must reject the request.

Rules:
- Use Python pytest.
- Use the requests library.
- Include a BASE_URL constant.
- Use clear test function names.
- For each test function, add a one-line docstring describing the test scenario.
- Validate status code.
- Validate important response body fields when the API returns a successful response.
- Correctly handle nested JSON fields.
- Include at least one positive test.
- Include edge case tests where useful.
- Do not invent fields that are not present in the API contract.
- Add comments for assumptions.
- Prefer helper functions or fixtures if they improve readability.
- Prefer parametrization when it makes the test suite clearer.
- Output only valid Python code. Do not include markdown fences.

Requirement:
{requirement}

API Contract:
{api_contract}